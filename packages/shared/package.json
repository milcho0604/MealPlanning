{
  "name": "@mealplan/shared",
  "version": "0.0.1",
  "description": "모바일 앱과 백엔드 API가 공통으로 사용하는 타입, 상수, 유틸 패키지",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
