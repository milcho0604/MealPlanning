name: Render 서버 슬립 방지

on:
  schedule:
    - cron: '*/4 * * * *'  # 4분마다 실행
  workflow_dispatch:        # 수동 실행 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API 서버
        run: |
          curl -f https://mealplan-api-dtx0.onrender.com/health || echo "Ping failed"
